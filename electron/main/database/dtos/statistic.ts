export type CategorySum = {
    id: number; // Category Id
    name: string;
    value: string; // Category value
    total: number;
};

export type DayExpenseSum = {
    spentDate: string;
    total: number;
};
